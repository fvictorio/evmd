{
  "name": "evmd",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter playground dev",
    "deploy": "./scripts/deploy.sh"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
